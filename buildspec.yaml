version: 0.2

phases:
  pre_build:
    commands:
      - cd frontend
      - npm i --legacy-peer-deps
  build:
    commands:
      - npm run build

artifacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: frontend/build
